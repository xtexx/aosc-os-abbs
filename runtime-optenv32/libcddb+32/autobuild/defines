PKGNAME=libcddb+32
PKGSEC=libs
PKGDEP="aosc-aaa+32 glibc+32"
BUILDDEP="devel-base+32"
PKGDES="Library that implements the different protocols (CDDBP, HTTP, SMTP) to access data on a CDDB server (e.g. http://freedb.org) (32-bit x86 runtime)"

ABHOST=optenv32
